from statistics import *

def get_media(lista):
    num = mean(lista)
    return num

def get_mediana(lista):
    num = median(lista)
    return num

def get_moda(lista):
    num = mode(lista)
    return num